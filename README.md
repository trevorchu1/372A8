# React RPS
In this assignment, we created a more complex version of the rock paper scissors game using react.
Most of the logic remains the same, and just needed to be ported.

To run the application, run npm install && npm run dev from the project root.
